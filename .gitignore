node_modules/
dist/
build/
.DS_Store
.env
.env.*
*.local
*.log
.vite/
*.tsbuildinfo

# Estado de execucao local do workspace (PIDs, pastas, historico de acesso) --
# nao faz sentido compartilhar entre maquinas diferentes.
app/server/data/

# Backup do historico git antigo do weknow_ask (ver mensagem de commit)
weknow_ask-historico-antigo.bundle

# Site estatico publicado no GitHub Pages -- gerado pelo workflow
# (.github/workflows/pages.yml) a cada push, nao faz sentido versionar.
/_site/
